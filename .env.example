# Copy only the variables you need into `.env`; `.env` is ignored by Git.
# Use command names or machine-local paths. Never commit real paths or secrets.
BROWSERCTL_RUN_DIR=
CHROME_PATH=
BROWSERCTL_PI_BIN=pi
BROWSERCTL_CODEX_BIN=codex
BROWSERCTL_CLAUDE_BIN=claude
EDITOR=
