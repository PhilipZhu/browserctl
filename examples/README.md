# Examples

Each example is a small application-owned run directory built on the reusable
browserctl host. Start with [page-label](page-label/README.md).

Examples keep their generated sessions, browser profiles, screenshots, and prompt
templates under their own directory. Those runtime files are ignored by Git.
