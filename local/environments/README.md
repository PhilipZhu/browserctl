# Local Environments

Place machine-specific environment files, executable wrappers, and local runtime
configuration here. Files in this folder are ignored by Git; only this README is
public. Never store credentials in a tracked example file.
